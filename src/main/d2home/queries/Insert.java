package main.d2home.queries;

import javax.swing.*;

public class Insert extends JFrame {

    private JPanel mainPanel;

    public Insert(String user, String password) {
        setContentPane(mainPanel);
        setTitle("Insert");
        setVisible(true);
        setBounds(600, 300, 320, 256);
    }

}
